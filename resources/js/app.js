import Alpine from 'alpinejs';

// Inisialisasi Alpine.js global untuk sidebar toggle (hamburger/off-canvas)
// dan dropdown profil navbar di seluruh aplikasi — bukan hanya landing page.
window.Alpine = Alpine;
Alpine.start();
